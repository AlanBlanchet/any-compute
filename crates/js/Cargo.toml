[package]
name = "any-compute-js"
description = "V8-like JavaScript engine optimized for any-compute — lexer, parser, bytecode compiler, register VM."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
any-compute-core = { workspace = true }
log.workspace = true
